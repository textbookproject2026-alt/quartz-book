# The Three Domains

A page whose name has spaces and capitals. On Publish it was
`/chapters/Definitions/The+Three+Domains`; Quartz serves it at
`/chapters/definitions/the-three-domains`, so the builder writes a redirect in both
the `+` and `%20` spellings.
